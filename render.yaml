# Blueprint для Render (опционально). Главное для 404 при F5 — client/public/_redirects
staticSites:
  - name: social-boost
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
